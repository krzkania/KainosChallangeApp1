@model List<KainosApp1.Models.CoinsLTC_daily>
@{
    var myChart = new Chart(width: 1200, height: 800)
        .AddTitle("daily LTC")

        .AddSeries(
            name: "LITECOIN",
            chartType: "line",
            xValue: Model, xField: "time",
            yValues: Model, yFields: "average")
        .Write();
}